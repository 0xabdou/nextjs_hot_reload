import styles from '../styles/Home.module.scss';

const Home = () => {
  return (
    <div className={styles.container}>
      <img className={styles.logo} src='next.png' alt='next.js logo'/>
      <p className={styles.label}>Enhanced boilerplate</p>
    </div>
  );
};

export default Home;

